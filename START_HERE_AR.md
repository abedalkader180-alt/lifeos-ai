# 🚀 LifeOS AI — ابدأ من هنا (خطوة بخطوة)

> هذه الوثيقة تشرح بالضبط: مفتاح ماذا؟ من أين أخذته؟ وأين أضعه؟
> اتبع الخطوات بالترتيب، كل خطوة مدتها ١-٢ دقيقة.

---

## 1) أولاً: مفتاح الـ API — ماذا يعني؟

الـ **API Key** هو "كلمة سر" صغيرة يمنحك إياها مزوّد الذكاء الاصطناعي، حتى يستطيع موقعك أن يرسل أسئلة المستخدمين إلى ذكاء اصطناعي حقيقي ويجيبهم.

- **بدونه**: المساعد يعمل بذكاء "قواعد" بسيط (جيد للتجربة، لكن ليس ذكاءً حقيقياً).
- **معه**: يستخدم ذكاء اصطناعياً حقيقياً يقرأ رسالة المستخدم بالعربية/الإنجليزية ويبني له خطة فعلية.

لا تشارك المفتاح مع أي شخص أبداً. هو مثل كلمة مرور حسابك.

---

## 2) اختر الخيار المناسب لك

| الخيار | التكلفة | يحتاج بطاقة؟ | الجودة | التوصية |
|---|---|---|---|---|
| **Groq** | مجاني (لمشروع البداية) | لا | جيدة جداً وسريعة | ✅ **الأنسب لك الآن** |
| **OpenRouter (نماذج free)** | مجاني (بحدود يومية) | لا | جيدة | ✅ بديل مجاني ثاني |
| **OpenAI** | مستخدم (حوالي $0.15-0.60 لكل مليون توكن) | نعم | الأعلى | لاحقاً عند النمو |

---

## 3) الخطوات: احصل على مفتاح Groq المجاني (الأنصح)

1. افتح: **https://console.groq.com**
2. أنشئ حساباً (أسهل طريقة: الدخول بـ **Google** أو **GitHub**).
3. بعد الدخول، من القائمة الجانبية اختر **API Keys**.
4. اضغط **Create API Key**.
5. أعطه أي اسم (مثل `lifeos`)، ثم اضغط **Submit**.
6. **انسخ المفتاح فوراً** — يبدأ بـ `gsk_` ويظهر مرة واحدة فقط.
7. لا تشاركه مع أحد.

### ما النموذج الذي أستخدمه مع Groq؟
- `llama-3.3-70b-versatile` (الأفضل للاستخدام العام)
- `llama-3.1-8b-instant` (أسرع وأرخص)
- يدعم العربية بشكل جيد.

> ملاحظة صادقة: خطة Groq المجانية محدودة السرعة (30 طلب/دقيقة و14,400 طلب/يوم تقريباً)، وهذا أكثر من كافٍ لبداية مشروعك.

---

## 4) ضع المفتاح في ملف الإعدادات

ملف الإعدادات هو `.env` (موجود داخل المشروع، ويتوقف عن الانتشار إلى GitHub — محمي في `.gitignore`).

افتح `.env` وعدّل هذه الأسطر:

```env
# ===== الذكاء الاصطناعي =====
AI_ENABLED=true

# ===== إذا استخدمت Groq =====
AI_API_KEY=gsk_ضع_المفتاح_هنا
AI_BASE_URL=https://api.groq.com/openai/v1
AI_MODEL=llama-3.3-70b-versatile
```

### إذا فضّلت OpenRouter (مجاني أيضاً):
```env
AI_ENABLED=true
AI_API_KEY=sk-or-v1-ضع_المفتاح_هنا
AI_BASE_URL=https://openrouter.ai/api/v1
AI_MODEL=nvidia/nemotron-3-ultra-550b-a55b:free
```
> انتبه: النماذج المجانية على OpenRouter تتغيّر من وقت لآخر. افحص صفحة "Free Models" لاختيار اسم نموذج ينتهي بـ `:free`.

### إذا فضّلت OpenAI (أقوى، لكن بدفع):
```env
AI_ENABLED=true
AI_API_KEY=sk-ضع_المفتاح_هنا
AI_BASE_URL=https://api.openai.com/v1
AI_MODEL=gpt-4o-mini
```

---

## 5) أعد تشغيل المشروع بعد تغيير الملف

الموقع يعمل حالياً في المعاينة على المنفذ `3000`. بعد تعديل `.env`:

- **في السيرفر المحلي**: اضغط `Ctrl+C` ثم أعد تشغيل أمر `npm start`.
- **في هذه الجلسة**: أخبرني "ضعت المفتاح" وأنا أعيد التشغيل وأتحقق أن الذكاء الحقيقي يعمل ✔

---

## 6) كيف أتأكد أن الذكاء الحقيقي يعمل؟

بعد إعادة التشغيل، تفتح `/app`، تسجّل دخول، تذهب إلى **AI Assistant**، وتكتب:
- بالعربية: *"أنا عندي ضغط بالشغل وما في وقت للعائلة، عملتلي خطة"*
- أو بالإنجليزية: *"I keep procrastinating. Build me a weekly plan"*

إذا رأيت رداً **طبيعياً فريداً ومفصّلاً** (ليس قالباً ثابتاً)، فالمفتاح يعمل.

---

## 7) الخطوات التالية (بعد إثبات أن الذكاء يعمل)

1. **ضع عنوان محفظتك الحقيقي** في `.env` داخل `TRUST_WALLET_ADDRESS=...` (من تطبيق Trust Wallet، شبكة TRON/TRC20).
2. **غيّر كلمة مرور المالك** في `.env` (`OWNER_PASSWORD`).
3. **غيّر `JWT_SECRET`** إلى سلسلة عشوائية طويلة.
4. **نشر المشروع** على استضافة مثل Render/Railway/Vercel (دليل في README).
5. **ابدأ التسويق الصادق**: صفحة الهبوط جاهزة، نضيف قسم أخبار/مدونة + قائمة انتظار + ربط غوغل أناليتكس.
6. **لوحة المالك** تتابع الإيرادات وكل الدفعات من `/admin`.

---

## 8) قاعدة أمان (اقرأها مرتين)
- ❌ لا ترفع `.env` إلى GitHub أبداً (محمي تلقائياً).
- ❌ لا ترسل **API Key** أو **Seed Phrase** للمحفظة لأي شخص (ولا حتى لي).
- ✅ تشارك فقط عنوان `TRUST_WALLET_ADDRESS` العام (الذي يبدأ بـ `T`)، فهو للاستلام فقط وليس مفتاحاً.

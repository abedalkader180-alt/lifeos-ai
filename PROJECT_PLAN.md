# LifeOS AI — خطة المشروع (Roadmap)

> قائد المشروع: وكيل Arena (قيادة تطوير + هندسة)
> الهدف: منتج حقيقي ينظّم حياة الناس، مربح، يقبل الدفع عبر Trust Wallet، ويُبنى على أساس صادق.

---

## 0) الوضع الحالي (واقعي) — ✅ تم البناء الأساسي
- ✅ نظام حقيقي: Express + SQLite (أو Turso عند التغيير) + JWT + bcrypt.
- ✅ تسجيل دخول وحسابات حقيقية + مهام + عادات + محادثات محفوظة.
- ✅ ذكاء اصطناعي حقيقي مفعّل عبر Groq (مفتاح `gsk_...` موجود في `.env` المحمي).
- ✅ خطة اشتراك (Free / Pro / Life) + دفع USDT (TRC20) عبر Trust Wallet + QR + Hash + تأكيد يدوي.
- ✅ لوحة مالك `/admin`: إيرادات، مستخدمين، تأكيد/رفض دفعات، ترقية الخطط.
- ✅ صفحة هبوط ثنائية اللغة (عربي/إنجليزي) بدون ادعاءات وهمية.
- ✅ إعداد النشر لـ **Vercel + Turso** (+ خيارات بديلة).

---

## 1) القرارات المُتخذة
1. اللغة: ثنائي اللغة (عربي/إنجليزي).
2. الذكاء: Groq (مفتاح موجود، نمط OpenAI-compatible).
3. شبكة الدفع: USDT TRC20 عبر Trust Wallet.
4. البنية: نظام حقيقي بقاعدة بيانات.

---

## 2) المرحلة 1 — أساس حقيقي ✅ (منجزة)
- [x] حذف الادعاءات المزيفة.
- [x] تسجيل دخول حقيقي وحفظ البيانات.
- [x] لوحة تحكم: مهام/عادات/ذكاء/اشتراك، تعمل وتُحفظ.
- [x] صفحة "كيف تعمل" + FAQ + خطة أسعار.
- [x] إعداد GitHub ونشر أول نسخة (فرع العمل).

## 3) المرحلة 2 — ذكاء اصطناعي حقيقي ✅ (منجزة)
- [x] ربط جروك (OpenAI-compatible).
- [x] مساعد "أرشيتكت الحياة" يبني خططاً (عربي/إنجليزي).
- [x] حدود استخدام حسب الخطة (مجاني 3/يوم، Pro غير محدود).
- [x] حفظ المحادثات.

## 4) المرحلة 3 — الدفع عبر Trust Wallet ✅
- [x] صفحة أسعار حقيقية.
- [x] زر الاشتراك يفتح صفحة دفع (USDT TRC20).
- [x] عرض عنوان المحفظة + QR.
- [x] إرسال Hash → تُقيد في النظام.
- [x] لوحة مشرف: تأكيد/رفض، تفعيل خطط، عرض الإيرادات.
- [ ] التحقق التلقائي من Hash عبر Explorer API (اختياري لاحقاً).

## 5) المرحلة 4 — النشر والتشغيل (المرحلة الحالية)
- [x] إعداد `.env.example` + توثيق.
- [ ] **تعبئة عنوان المحفظة الحقيقي** + كلمة مرور مالك جديدة.
- [ ] **إنشاء قاعدة Turso مجانية** وإدخال رابطها وتوكنها في الإعدادات.
- [ ] تعديل طبقة البيانات لدعم Turso (البرمجة).
- [ ] إعداد واجهة Vercel + اسم النطاق.
- [ ] رفع المشروع والتحقق من الدفع الفعلي.

## 6) المرحلة 5 — أول عملاء وأرباح (بعد النشر)
- [ ] 10 منشورات/فيديوهات قصيرة (يعدّها المساعد التسويقي).
- [ ] كوبون تأسيسي (نصف سعر لأول 20).
- [ ] نظام إحالة.
- [ ] تجربة Pro مجانية 3 أيام مقابل الإيميل.
- [ ] تنبيه إيميل عند كل دفعة.
- [ ] تحليلات بسيطة.

## 7) حماية وامتثال
- [ ] سياسة خصوصية + شروط استخدام + استرداد.
- [ ] النسخ الاحتياطي.
- [ ] لا وعود دخل، لا Spam، لا أرقام وهمية.

---

## 8) قاعدة ذهبية
- لا وعود دخل مضمونة.
- لا أرقام مستخدمين وهمية.
- لا رسائل تلقائية غير مرغوبة (Spam).
- المال لا يصل إلا عندما يدفع عميل حقيقي مقابل خدمة حقيقية.

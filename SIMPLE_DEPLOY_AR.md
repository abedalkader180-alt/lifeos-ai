# 🖱️ الدليل البسيط: انشر موقعك (بنقر واحد للقيم بدل 7 متغيرات)

> **الأمر أسهل الآن:** لا تحتاج إضافة 7 متغيرات.
> تحتاج فقط **متغيراً واحداً** اسمه `LIFEOS_SETTINGS` تلصق فيه قيمة واحدة جاهزة تحتوي كل شيء.
> اضغط بالترتيب أدناه.

---

## 📌 القيمة الجاهزة التي تلصقها (انسخها كلها إلى آخرها)

> ⚠️ هذه القيمة **تحوي بيانات حساسة** (قاعدة البيانات، المحفظة، المفتاح، المالك).
> لذلك **لا أضعها هنا في ملف مرفوع** حتى لا تتسرب إلى GitHub.
> سأمرر لك النسخة الأصلية في **المحادثة** فقط. الملف هنا يشرح المكان/الاسم/الشكل.

```
LIFEOS_SETTINGS={"DATABASE_URL":"<قاعدة البيانات>","TRUST_WALLET_ADDRESS":"T...","AI_ENABLED":"true","AI_API_KEY":"gsk_...","OWNER_EMAIL":"owner@lifeos.app","OWNER_PASSWORD":"كلمة مرورك","JWT_SECRET":"سر عشوائي طويل"}
```

> اشرب إلى محادثة المساعد ("مرر لي القيمة الجاهزة لنشر LifeOS") وسأعطيك النسخة الفعلية الجاهزة للصق والتي تحتوي كل بياناتك.

---

## 🧭 الخطوات بالترتيب

1. افتح `vercel.com` وسجّل دخولك.
2. اضغط **Add New…** (أعلى اليمين) ← **Project**.
3. اختر **Import Git Repository** ← اختر مستودع `lifeos-ai`.
4. اختر فرع `arena/01a07754-lifeos-ai` (أو main).
5. قبل زر Deploy، ابحث عن قسم **Environment Variables**.
6. اضغط **Add**.
7. في خانة **Name**: اكتب `LIFEOS_SETTINGS`
8. في خانة **Value**: الصق القيمة الجاهزة أعلاه (الصقها كلها كما هي).
9. اضغط **Deploy** (الزر الأخضر).

---

## ✅ بعد النشر
- ستحصل على رابط `https://....vercel.app`.
- أرسل لي هذا الرابط، وسأفحص كل شيء (القاعدة، المحفظة، الذكاء، الدفع) وأبدأ خطة الدخل.

---

## ❓ ماذا لو لم تجد "Environment Variables"؟
في صفحة الإعداد بعد ربط المستودع، عادةً تظهر تحت "Build and Output Settings" — وإن لم تجدها، اضغط زر **Deploy** أولاً، وسلنّخصه على GitHub:

> يمكنك أيضاً ببساطة بعد إنشاء المشروع:
> **Settings → Environment Variables → Add New**، املأ الاسم والقيمة، ثم **Redeploy**.

---

## 🔒 ملاحظات أمان
- لا تعرض هذا الملف/القيمة في مكان عام (بما فيه تويت أو مجموعة).
- بعد النشر، يمكنك إنشاء متغيرات منفصلة لاحقاً إذا أردت.

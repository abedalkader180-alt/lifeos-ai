# 🌍 خيارات النشر (ما الذي يناسب مشروعنا؟)

## المشكلة
LiveOS AI يحتاج:
1. تشغيل الموقع دائماً.
2. **قاعدة بيانات دائمة** (حسابات، مهام، مدفوعات).
3. اتصال بـ Groq (الذكاء الحقيقي).

## الخيار 1 — الأنسب لك حالياً (لديك حساب Vercel)
**نشر على Vercel + Turso (قاعدة بيانات سحابية مجانية).**
- ✅ يستخدم حساب Vercel الذي تملكه.
- ✅ قاعدة بيانات مجانية دائمة لا تختفي.
- ✅ يعمل مع اللهجة (Node serverless).
- ✅ أدوات: ندعمه في الكود ونشرح خطوات الرفع.
- ❗ يحتاج: رابط قاعدة بيانات Turso + توكن Turso (ستوضحه لك خطوة بخطوة).

## الخيار 2 — أبسط تقنياً (إن لم ترد Turso)
**نشر على Render (مجاني) أو Railway.**
- ✅ مشروع Node كامل يعمل كما هو بدون تغيير.
- ✅ أسرع للتشغيل.
- ❗ الحاجة: حساب جديد على Render/Railway (بعضها يحطل بطاقة، بعضه لا).
- ⚠️ لكن على الخطة المجانية قد لا تكون قاعدة البيانات دائمة 100%، لذلك نستخدم ديسك مدفوع لاحقاً أو Turso أيضاً.

## الخيار 3 — الأقوى للأرباح لاحقاً
**سيرفر شخصي صغير (VPS) من Hostinger/Contabo/أي مزوّد.**
- ✅ تحكم كامل، قاعدة بيانات دائمة، إيميل مخصص، دومين.
- ✅ مثالي لما يبدأ نسبو المدفوعات.
- ❗ يكلف قليل (3-8$ شهرياً).
- 💡 أوصي بهذا بعد أن نتحقق أن فعلاً هناك دفع.

---

## ماذا أحتاج منك لتجهيز النشر (الخيار 1 الآن)؟
1. **رابط قاعدة البيانات** من Turso (سيظهر مثلاً `libsql://...`).
2. **توكن** من Turso (سلسلة طويلة تبدأ بـ `eyJ...`).
3. **عنوان المحفظة TRC20 الحقيقي** الخاص بك (من Trust Wallet، يبدأ بـ `T`).
4. **إيميل + كلمة مرور المالك** (للوحة `/admin`، اخترها بنفسك).

> لا تشارك Seed Phrase أبداً. العنوان العام `T...` هو للاستلام فقط.
